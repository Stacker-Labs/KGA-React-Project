import React from "react";
import { Logo_font } from "../../fonts";

const Logo = () => {
  return (
    <p className="`font-{Logo_font}` text-4xl text-accent-blue">Stacker-Labs</p>
  );
};

export default Logo;
